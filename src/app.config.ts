export default defineAppConfig({
  ui: {
    colors: {
      primary: 'primary',
      neutral: 'zinc',
    },
    container: {
      base: 'w-full max-w-(--ui-container) mx-auto p-5',
    },
  },
})
